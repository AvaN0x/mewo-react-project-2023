export const login = async ({
	email,
	password,
}: {
	email: string;
	password: string;
}) => {
	// TODO
};

export const register = async ({
	name,
	email,
	password,
}: {
	name: string;
	email: string;
	password: string;
}) => {
	// TODO
};
